# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build now outputs to ../assiduous_dash/frontend_dist (see
# vite.config.ts) instead of dist/ here — Railway's build for the
# backend service only packages the assiduous_dash/ subtree, so the
# committed build output has to live inside it. See the .gitignore
# comment there.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
